CREATE PROCEDURE [dbo].[procCIO_PERSONNEL_Insert]
(
@fname varchar(50), 
@lname varchar(50), 
@mname varchar(50), 
@title varchar(50), 
@email varchar(100), 
@loc varchar(100), 
@ph varchar(50), 
@ext varchar(10),
@comm varchar(8000), 
@pager varchar(50),
@alt varchar(50), 
@login varchar(25),
@ssn varchar(11),
@rnk int,
@rate varchar(15),
@aud bigint,
@dob datetime,
@med varchar(20),
@oth varchar(50),
@nmci varchar(100),
@cit int,
@cby int,
@eaos datetime,
@sex varchar(1),
@acc varchar(20),
@ver varchar(20),
@add1 varchar(100),
@add2 varchar(100),
@city varchar(50),
@state varchar(2),
@zip varchar(11),
@des int,
@net bit,
@out bit,
@prev varchar(50),
@ee datetime,
@usr int,
@cont varchar(50),
@cnum varchar(50),
@hcs int,
@eth int,
@ahlta varchar(20),
@bas int,
@displ varchar(150),
@cidt datetime,
@sufx varchar(6) = '',
@comp int = 0,
@supe varchar(150) = '',
@edu int = 0,
@dod nvarchar(10),
@eff datetime = '1/1/1776',
@npi numeric(16,3) = 0
)
 AS

INSERT INTO PERSONNEL
(
FirstName, 
LastName, 
MiddleName, 
Title, 
EMailAddress, 
Location, 
UPhone, 
Extension, 
Comments, 
UPager,
AltPhone, 
LoginId, 
SSN,
RankId,
Rate,
AudienceId,
DOB,
MedStuYr,
OtherStu,
NMCIEMail,
CitizenshipId,
CreatedBy,
EAOS_PRD,
Sex,
ACCESSCODE,
VERIFYCODE,
Address1,
Address2,
City,
[State],
Zip,
DesignationId,
NetworkAccess,
OutlookExchange,
PreviousDutyStation,
ExpectedEndDate,
UserId,
ContractorCompany,
ContractNumber,
HealthcareStatusId,
EthnicityId,
AHLTA,
BaseId,
DisplayName,
CheckInDate,
ComponentId,
Suffix,
Supervisor,
EducationLevelId,
DoDEDI,
EffectiveDate,
NPIKey
) 
VALUES(
@fname, 
@lname, 
@mname, 
@title, 
@email, 
@loc, 
@ph, 
@ext, 
@comm, 
@pager,
@alt,
@login,
@ssn,
@rnk,
@rate,
@aud,
@dob,
@med,
@oth,
@nmci,
@cit,
@cby,
@eaos,
@sex,
@acc,
@ver,
@add1,
@add2,
@city,
@state,
@zip,
@des,
@net,
@out,
@prev,
@ee,
@usr,
@cont,
@cnum,
@hcs,
@eth,
@ahlta,
@bas,
@displ,
@cidt,
@comp,
@sufx,
@supe,
@edu,
@dod,
@eff,
@npi
);
SELECT SCOPE_IDENTITY();


