CREATE TABLE [dbo].[RESPONSIBILITY] (
    [ResponsibilityId]   INT           NOT NULL,
    [ResponsibilityDesc] NVARCHAR (50) NULL,
    CONSTRAINT [PK_RESPONSIBILITY] PRIMARY KEY CLUSTERED ([ResponsibilityId] ASC)
);

