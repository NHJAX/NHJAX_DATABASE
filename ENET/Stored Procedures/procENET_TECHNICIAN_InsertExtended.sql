CREATE PROCEDURE [dbo].[procENET_TECHNICIAN_InsertExtended]
(
@fname varchar(50), 
@lname varchar(50), 
@mname varchar(50), 
@title varchar(50), 
@email varchar(250), 
@loc varchar(100), 
@ph varchar(50), 
@ext varchar(10),
@comm varchar(8000), 
@pager varchar(50),
@alt varchar(50), 
@login varchar(256),
@cby int,
@ssn varchar(11),
@rnk int,
@aud bigint,
@dob datetime,
@rate varchar(15),
@med varchar(20),
@oth varchar(50),
@eaos datetime,
@sex varchar(1),
@cit int,
@src int,
@nmci varchar(100),
@add1 varchar(100),
@add2 varchar(100),
@city varchar(50),
@state varchar(2),
@zip varchar(11),
@des int,
@prev varchar(50),
@ee datetime,
@cont varchar(50),
@cnum varchar(50),
@hcs int,
@bas int,
@net bit,
@out bit,
@eth int,
@displ varchar(150),
@cidt datetime,
@sufx varchar(6) = '',
@comp int = 0,
@supe varchar(150) = '',
@edu int = 0,
@dod nvarchar(10),
@eff datetime = '1/1/1776',
@emp int = 0
)
 AS

INSERT INTO TECHNICIAN
(
UFName, 
ULName, 
UMName, 
Title, 
EMailAddress, 
Location, 
UPhone, 
Extension, 
Comments, 
UPager,
AltPhone, 
LoginId,
CreatedBy, 
SSN,
RankId,
AudienceId,
DOB,
Rate,
MedStuYr,
OtherStu,
EAOS_PRD,
Sex,
CitizenshipId,
SourceSystemId,
NMCIEMail,
Address1,
Address2,
City,
[State],
Zip,
DesignationId,
PreviousDutyStation,
ExpectedEndDate,
ContractorCompany,
ContractNumber,
HealthcareStatusId,
BaseId,
NetworkAccess,
OutlookExchange,
EthnicityId,
DisplayName,
CheckInDate,
Suffix,
ComponentId,
Supervisor,
EducationLevelId,
DoDEDI,
EffectiveDate,
EmployeeNumber
) 
VALUES
(
@fname, 
@lname, 
@mname, 
@title, 
RTRIM(LTRIM(@email)), 
@loc, 
@ph, 
@ext,
@comm, 
@pager,
@alt, 
@login,
@cby,
@ssn,
@rnk,
@aud,
@dob,
@rate,
@med,
@oth,
@eaos,
@sex,
@cit,
@src,
@nmci,
@add1,
@add2,
@city,
@state,
@zip,
@des,
@prev,
@ee,
@cont,
@cnum,
@hcs,
@bas,
@net,
@out,
@eth,
@displ,
@cidt,
@sufx,
@comp,
@supe,
@edu,
@dod,
@eff,
@emp
);
SELECT SCOPE_IDENTITY();
